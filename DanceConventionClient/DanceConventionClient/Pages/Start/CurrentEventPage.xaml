<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DanceConventionClient.Pages.CurrentEventPage"
			 Title="Event">

	<StackLayout>
		<Label Text="{Binding CurrentEvent.Name}"
			   FontSize="20"
			   FontAttributes="Bold"
			   HorizontalOptions="CenterAndExpand"
			   TextColor="Black"/>
		
		<Button Text="My registration"
				Command="{Binding MyRegistrationCommand}"
				IsVisible="{Binding IsCompetitor}"
				Style="{StaticResource ButtonStyle}"/>
		
		<Button Text="Contest check-in"				
				IsVisible="{Binding IsStaff}"
				Style="{StaticResource ButtonStyle}"/>
		
		<Button Text="Registration desk"				
				IsVisible="{Binding IsStaff}"
				Style="{StaticResource ButtonStyle}"
				Command="{Binding RegistrationCommand}"/>
	</StackLayout>
</ContentPage>